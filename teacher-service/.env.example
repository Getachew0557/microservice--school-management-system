# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=teacher_db
DB_POOL_LIMIT=10

# Test Database Configuration (used when NODE_ENV=test)
TEST_DB_HOST=localhost
TEST_DB_PORT=3306
TEST_DB_USER=root
TEST_DB_PASSWORD=
TEST_DB_NAME=test_teacher_db

# JWT (for future)
JWT_SECRET=
JWT_EXPIRE=24h